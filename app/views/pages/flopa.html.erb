<h2>You are in <strong>Flo</strong>wers <strong>Pa</strong>radise!</h2>

<% @products.each do |product| %>
    <div class="grid-container">
        <div class="grid-item">
            <%= link_to image_tag(product.image, :class => "thumb"), product %>
            <p><%= product.name %></p>
        </div>
    </div>
<% end %> 